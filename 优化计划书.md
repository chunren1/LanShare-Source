# LanShare 局域网文件共享 · 优化计划书

> 版本：v1.1 ｜ 日期：2026-08-10 ｜ 状态：**P0 已完成（2026-08-10）**，P1/P2 待实施
> 定位：本文档是 LanShare 在 **下载性能、用户体验、界面视觉** 三大方向上的优化方案与实施计划，不含架构级重写，所有改动在现有 FastAPI + Vue3 技术栈内完成。

---

## 目录

1. [项目现状与问题诊断](#一项目现状与问题诊断)
2. [优化目标与度量指标](#二优化目标与度量指标)
3. [优化方向一：下载更快](#三优化方向一下载更快)
4. [优化方向二：体验更好](#四优化方向二体验更好)
5. [优化方向三：界面更美](#五优化方向三界面更美)
6. [分阶段实施路线图](#六分阶段实施路线图)
7. [验收标准与回归测试](#七验收标准与回归测试)
8. [风险控制与依赖](#八风险控制与依赖)

---

## 一、项目现状与问题诊断

### 1.1 项目概况

| 项目 | 说明 |
| --- | --- |
| 技术栈 | 后端 FastAPI + uvicorn + SQLite；前端 Vue 3 + Vite + Element Plus（单页无路由库） |
| 核心能力 | 分片上传 / 秒传 / 断点续传、HTTP Range 下载、ZIP 打包下载、图片/视频/音频/PDF/文本在线预览、二维码访问、访问密码 |
| 架构 | 后端三层：`routers`（HTTP 层）→ `services`（业务层）→ `config/database/utils`（基础层）；前端 `App.vue` 单挂载点 |
| 数据 | 文件在 `data/files/`，分片临时文件在 `data/tmp/`，元数据在 SQLite `data/lanshare.db` |

### 1.2 现状问题诊断

#### A. 下载性能瓶颈

| 问题 | 现状表现 | 影响 |
| --- | --- | --- |
| 单连接下载 | 前端通过 `window.location.href` 直接跳转下载链接，浏览器单 TCP 连接 | 千兆局域网下单连接无法充分利用带宽；**无下载进度提示**，大文件"点了没反应" |
| ZIP 同步打包 | `build_zip_file()` 先写完整临时文件再响应（`ZIP_STORED`） | 多文件/大目录打包时**首字节延迟 = 整个打包耗时**，用户长时间无反馈 |
| API 响应未压缩 | JSON 列表/搜索结果原样传输 | 大目录列表几百 KB 需重复传输，移动网络下感知慢 |
| 存储统计全量遍历 | `storage_info()` 每次 `rglob` 遍历整个文件树 | 万级文件目录下耗时数秒，且每次刷新重复计算 |

#### B. 用户体验缺口

| 问题 | 现状表现 |
| --- | --- |
| 无文件夹上传 | 文件选择框未启用 `webkitdirectory`，需逐个选文件 |
| 传输无全局视图 | 上传队列局限于弹窗内，关闭即清空；下载完全无界面反馈 |
| 上传任务不可恢复 | 页面刷新后未完成的 `upload_tasks` 无前端恢复入口 |
| 预览能力单薄 | 图片不能缩放/旋转/翻页；文本无编码处理；不可复制预览链接 |
| 操作效率低 | 无快捷键、无搜索防抖、无批量"全选/全不选" |

#### C. 界面视觉问题

| 问题 | 现状表现 |
| --- | --- |
| 视觉风格陈旧 | 使用 Element Plus 默认蓝主题，纯白底 + 细线表格，无品牌感 |
| 仅表格视图 | 无网格/缩略图视图，图片类文件无预览图 |
| 无深色模式 | 夜间使用刺眼 |
| 信息呈现单调 | footer 只有一行文字；无存储可视化、无空状态插画、无动效 |
| 移动端粗糙 | 操作列固定宽度 200px，小屏拥挤，仅靠 `hide-on-mobile` 隐藏次要列 |

### 1.3 根因归纳

- **下载慢** 根因在"浏览器跳转下载"模型：无法并发、无法显示进度、无法恢复，属于前端模型问题，后端 Range 已就绪；
- **ZIP 慢** 根因在"先完整打包再发送"的服务端同步阻塞模型；
- **体验/视觉** 根因在功能完整但交互与呈现层缺少打磨。

---

## 二、优化目标与度量指标

### 2.1 总体目标

> 一句话：**让 LanShare 的下载"更快、看得见、可恢复"，界面"更好看、更顺手、更懂移动端"**，同时保持"轻量、免外网、免安装"的定位不变。

### 2.2 度量指标（可量化验收）

| 指标 | 当前基线 | 优化目标 | 验证方式 |
| --- | --- | --- | --- |
| 单文件大文件下载 | 单连接，无进度 | 并发分片下载，界面实时显示进度/速度；千兆局域网下多连接实测吞吐量提升（预期 ≥1.5x，视环境） | 实测 1GB 文件对比 |
| ZIP 首字节响应时间 | 等于整个打包耗时 | 流式打包：首字节在打包开始后立即发出，用户无需等待 | 请求大目录观察首字节 |
| 存储统计接口耗时 | 万级文件数秒级 | 增量缓存后毫秒级 | 压测大目录 |
| 上传文件夹 | 不支持 | 支持嵌套目录一键上传 | 手动验证 |
| 上传任务恢复 | 刷新即丢失 | 刷新后提示并恢复未完成任务 | 中断后刷新验证 |
| 深色模式 | 无 | 一键切换并持久化 | 手动验证 |
| 视图切换 | 仅表格 | 表格 / 网格双视图 | 手动验证 |
| 回归测试 | 16 项全绿 | 新增流式 ZIP 等测试后 **全量 ≥ 20 项全绿** | `pytest tests/ -v` |

### 2.3 原则

1. **不动架构**：保持单页结构、三层后端、SQLite 无 ORM；不引入路由库、不引入重量级图表库（图表用轻量方案或手写 SVG）。
2. **兼容优先**：下载加速在浏览器支持时启用，不支持时优雅回退到原跳转下载。
3. **测试先行**：所有后端改动同步更新 `tests/test_api.py`，保持隔离数据目录模式。

---

## 三、优化方向一：下载更快

### 3.1 前端并发 Range 分片下载器（核心）

**现状**：前端用 `window.location.href` 跳转 `withToken(downloadUrl(path))`，浏览器接管下载，无进度、无并发、失败不可见。

**方案**：新增 `frontend/src/utils/downloader.js`：

- 用 `fetch` + `Range: bytes=start-end` 头将文件切成 N 片（默认 4，随文件大小调整）并发拉取；
- 各分片用 `Blob` 拼接，完成后触发 `<a download>` 保存，文件名沿用后端 `filename*=UTF-8''`；
- 回调透出 `progress`（已下载字节数）、`speed`（实时速度）、`stage`（状态）；
- 支持 **暂停/继续**（`AbortController` 中止未完成分片）与 **失败自动重试**（单分片重试 2 次）；
- 分片下载 URL 复用现有 `withToken()` 机制，后端 `FileResponse` 原生支持 Range，**无需改后端**；
- 能力检测：不支持（如部分移动端 WebView）时回退原跳转下载。

**涉及文件**：
- 新增 `frontend/src/utils/downloader.js`
- 修改 `frontend/src/api/index.js`（新增 `downloadStreamUrl` 或复用）
- 修改 `frontend/src/App.vue`（下载按钮接入 + 进度回调）

**工作量**：约 1 天｜**风险**：低（后端零改动，纯前端增强，有回退路径）。

### 3.2 ZIP 流式打包（边打包边发送）

**现状**：`download_service.py::build_zip_file()` 同步把全部文件写入临时 ZIP 后，`download.py` 才用 `FileResponse` 发送；大目录"打包完成前页面无响应"。

**方案**：

1. 新增可读流迭代器，将 `zipfile.ZipFile` 的输出（`ZIP_STORED` 保持不压缩）逐块读出；
2. 路由改用 `StreamingResponse` 边打包边发送，用户**立即看到下载开始**；
3. 保留并发优化：用 `ThreadPoolExecutor` 并行预读文件块进缓冲队列，摊薄磁盘 IO 等待；
4. 无 `Content-Length`（chunked 传输），局域网内可接受；单文件与常规下载路径不受影响；
5. 打包用临时文件+流式读（保留现有"不占内存"设计），完成后仍由 `BackgroundTasks` 清理。

**涉及文件**：
- 修改 `app/services/download_service.py`（新增 `iter_zip_stream()`）
- 修改 `app/routers/download.py`（ZIP 路由改用 `StreamingResponse`）

**工作量**：约 1 天｜**风险**：中（需新增流式测试，防止破坏大文件下载；需保证异常时清理临时文件）。

### 3.3 API 响应 gzip 压缩

**现状**：所有 JSON 响应未压缩。

**方案**：在 `app/main.py` 挂载 `GZipMiddleware(minimum_size=1024)`。仅压缩文本/JSON 类响应；静态文件与 `Range` 下载走 `FileResponse`，星鸣 `Accept-Encoding` 判定，**不影响大文件与已压缩资源**。

**涉及文件**：修改 `app/main.py`

**工作量**：0.5 小时｜**风险**：低。

### 3.4 存储统计增量缓存

**现状**：`file_service.py::storage_info()` 每次 `rglob` 全量遍历文件树，大目录下每次刷新都卡顿。

**方案**：

- 进程内维护 `{目录路径: (计算时刻, 总大小)}` 缓存；
- 失效策略：目录 `mtime` 变化或超过 30s 时重算；上传/删除/重命名/合并成功时主动失效对应目录；
- 返回 `cache: true/false` 字段供前端感知（可选）。

**涉及文件**：修改 `app/services/file_service.py`

**工作量**：0.5 天｜**风险**：低（需注意多进程部署下缓存一致性，本项目单进程足够）。

### 3.5 下载配套体验

- 文件行/多选区增加"复制下载链接"（带 token）；
- 双击行 = 下载文件、进入目录（现有双击逻辑仅目录进入，扩展文件快速下载）；
- 下载中在传输中心展示（见 4.2）。

---

## 四、优化方向二：体验更好

### 4.1 文件夹上传

**现状**：`UploadPanel.vue` 文件选择框不支持目录。

**方案**：

- 输入框加 `webkitdirectory` 与 `directory` 属性，弹窗内增加"上传文件夹"入口；
- 递归收集目录下全部文件，保留相对路径（`file.webkitRelativePath`），以 `targetDir/子目录` 作为上传目标目录；
- 分片上传流程完全复用现有 `uploader.js`，仅目标目录拼接变化；展示"文件夹名（N 个文件）"队列条目。

**涉及文件**：修改 `frontend/src/components/UploadPanel.vue`、`frontend/src/utils/uploader.js`

**工作量**：0.5 天｜**风险**：低（后端 `upload/merge` 已支持 `targetDir` 嵌套）。

### 4.2 全局传输中心（上传/下载统一面板）

**现状**：上传队列在弹窗内、关闭即清空；下载完全无 UI。

**方案**：新建 `frontend/src/components/TransferCenter.vue` 悬浮面板（右下角浮动胶囊按钮展开）：

- 聚合全部传输任务：上传（来自 UploadPanel 队列）与下载（来自 3.1 下载器）；
- 每条任务展示：文件名、状态（等待/计算 MD5/上传中/下载中/暂停/失败/完成）、进度条、实时速度、剩余时间；
- 上传任务支持 **暂停**（中止未发送分片）、**取消**、**失败重试**（重新走 init 续传）；
- 全局状态用轻量 `reactive` 模块（`frontend/src/utils/transfers.js`）管理，弹窗与主界面共享；
- 上传完成后系统通知（浏览器 `Notification`，需授权，失败静默）。

**涉及文件**：
- 新增 `frontend/src/components/TransferCenter.vue`、`frontend/src/utils/transfers.js`
- 修改 `frontend/src/App.vue`（挂载面板）、`frontend/src/components/UploadPanel.vue`（队列接入全局状态）

**工作量**：1.5 天｜**风险**：中（状态管理需理清与现有弹窗队列的关系；下载进度依赖 3.1）。

### 4.3 刷新后上传任务恢复

**现状**：前端刷新后 `upload_tasks` 遗留的未完成任务无恢复入口。

**方案**：

- 应用启动时调用现有 `GET /api/upload/status`（或新增"列出未完成任务"接口）检测遗留任务；
- 发现未完成任务时提示"检测到 N 个未完成上传，是否续传？"，用户确认后按原有流程恢复（`uploader.js` 已支持断点续传，通过 init 返回的 `uploadedChunks` 跳过已传分片）；
- 注意：恢复需要原文件，文件不存在时自动清理失效任务。

**涉及文件**：修改 `frontend/src/App.vue`、`frontend/src/utils/uploader.js`；如新增接口则改 `app/routers/upload.py`

**工作量**：0.5~1 天｜**风险**：低-中（需处理"文件已不在本地"边界）。

### 4.4 预览增强

**现状**：`App.vue` 预览弹窗支持图片/视频/音频/PDF/文本，但图片不能缩放、多图不能翻页。

**方案**：

- 图片：接入 `ElImageViewer`（缩放/旋转/全屏），并支持同目录相邻图片左右切换（收集当前目录所有图片项）；
- 文本：超过 200KB 的提示条保留，补充"复制全部内容"按钮与 UTF-8/GBK 探测（后端返回前尝试解码，失败降级）；
- 预览弹窗增加"下载当前文件"快捷按钮。

**涉及文件**：修改 `frontend/src/App.vue`

**工作量**：0.5 天｜**风险**：低。

### 4.5 交互效率细节

| 能力 | 方案 |
| --- | --- |
| 快捷键 | `Ctrl/Cmd+F` 聚焦搜索、`Ctrl/Cmd+U` 打开上传、`Delete` 删除选中（需焦点不在输入框） |
| 搜索防抖 | 输入即搜（300ms 防抖），回车兜底；搜索时显示"在 X 个结果中" |
| 全选 | 工具栏增加"全选/取消"按钮（与表格 selection 联动） |
| 返回上级 | 面包屑支持点击上级；新增"向上"按钮（移动端友好） |
| 右键菜单 | 桌面端行右键弹出操作菜单（复制链接/重命名/删除/下载） |

**涉及文件**：修改 `frontend/src/App.vue`

**工作量**：0.5~1 天｜**风险**：低。

---

## 五、优化方向三：界面更美

### 5.1 视觉重构（品牌化）

**现状**：Element Plus 默认蓝 + 白底细线表格。

**方案**：

- **品牌色体系**：主色 `#2563EB`（科技蓝）→ 渐变 `#2563EB → #4F7CFF → #7C8CF8`（蓝紫渐变）；功能色 `#10B981` 成功 / `#F59E0B` 警告 / `#EF4444` 危险 / `#3B82F6` 信息；背景 `#F5F7FB`，正文 `#1F2937`，次要文字 `#6B7280`。
- **布局**：顶部改为渐变品牌头（柔和大面积渐变 + 阴影），标题与操作区统一；内容区卡片化（白色圆角卡片 + 12px 圆角 + 柔和阴影），留白 20px 规范。
- **组件统一**：通过 `element-plus` CSS 变量（`--el-color-primary` 等）统一主题，避免逐个组件硬编码颜色。
- **字体**：`PingFang SC, Helvetica Neue, Arial`；标题 20px/600，小标题 16px/500，正文 14px/400。

### 5.2 深色模式

**现状**：无。

**方案**：

- 全部颜色收敛为 CSS 变量（浅色/深色两套 token）；
- 顶栏加切换按钮：手动切换 + `prefers-color-scheme` 自动适配，选择持久化到 `localStorage`；
- 深色背景 `#0F1420`，正文 `#F3F4F6`，卡片 `#1A2234`；
- Element Plus 侧启用 `dark` class 配合变量覆盖。

### 5.3 网格视图（表格 / 网格切换）

**现状**：仅表格。

**方案**：

- 工具栏加"视图切换"（列表 / 网格图标）；
- 网格模式：卡片式文件块（大图标 + 文件名 + 大小），图片类文件显示**缩略图**；
- 缩略图：后端新增 `GET /api/thumbnail?path=` 接口（Pillow 生成 200px 缩略图，缓存到 `data/thumbnails/`，仅对图片类型）；前端网格加载时懒加载缩略图，无缩略图回退类型图标；
- 两种视图共享选中/搜索/操作逻辑。

**涉及文件**：
- 新增 `app/routers/thumbnail.py`（或并入 `files.py`）、`app/services/thumbnail_service.py`（Pillow，加入 `requirements.txt`）
- 修改 `frontend/src/App.vue`、新增 `frontend/src/components/FileGrid.vue`

**工作量**：1.5 天｜**风险**：中（Pillow 为新增依赖；缩略图需做路径安全校验复用 `safe_join`）。

### 5.4 存储仪表盘

**现状**：footer 一行文字（已用空间 / 项目数）。

**方案**：

- footer 升级为轻量仪表盘卡片：存储空间**环形进度**（手写 SVG，不引图表库）+ 文件/目录数量 + 最近更新时间；
- 可选：按文件类型分布的横向条形（图片/视频/压缩包/文档/其他），数据由扩展的 `/api/storage` 返回（沿用 3.4 缓存）。

**涉及文件**：修改 `app/services/file_service.py`（返回类型统计）、`frontend/src/App.vue`

**工作量**：0.5~1 天｜**风险**：低。

### 5.5 空状态与动效

- **空状态**：空目录/空搜索结果显示插画化提示（SVG 图形 + 引导文案 + 一键上传按钮），替代默认"暂无数据"；
- **动效**：列表行 hover 高亮与位移、进度条平滑过渡、弹窗淡入缩放（Element Plus 过渡 + 少量自定义 CSS）、拖拽上传高亮增强（全屏遮罩 + 动画）；
- 动效遵循"克制"原则，不引入重视觉负担。

### 5.6 移动端适配

- 操作列（预览/下载/重命名/删除）在小屏收纳为"更多"菜单，主行显示"预览/下载"两个高频操作；
- 保留并扩展 `hide-on-mobile` 机制隐藏次要列（大小/时间）；
- 工具栏自动换行，搜索框占满可用宽度；
- 上传/下载悬浮按钮与传输中心在移动端全屏化。

### 5.7 UI 设计规范（落地约束）

| 维度 | 规范 |
| --- | --- |
| 风格 | 现代简约 + 清爽科技感，卡片化、留白充足 |
| 主色 | `#2563EB`，渐变 `#2563EB → #4F7CFF → #7C8CF8` |
| 圆角 | 卡片 12px，按钮 8px，输入框 8px |
| 阴影 | `0 4px 16px rgba(37,99,235,.08)`（浅色）/ 深色加深 |
| 间距 | 页面边距 20px，卡片内边距 16~20px |
| 图标 | 统一 Element Plus 图标集 + 品牌 Logo SVG |

---

## 六、分阶段实施路线图

> 每个阶段结束均可独立上线，互不阻塞。

### Phase 0 —— 基础体验与下载提速（预计 3~4 天）

| # | 任务 | 归属 | 依赖 | 状态 |
| --- | --- | --- | --- | --- |
| 1 | 前端并发 Range 分片下载器 + 回退 | 3.1 | 无 | ✅ 2026-08-10 |
| 2 | ZIP 流式打包（后端 + 流式测试） | 3.2 | 无 | ✅ 2026-08-10 |
| 3 | API gzip 压缩 | 3.3 | 无 | ✅ 2026-08-10 |
| 4 | 存储统计增量缓存 | 3.4 | 无 | ✅ 2026-08-10 |
| 5 | 视觉重构（品牌色 + 卡片化 + CSS 变量化） | 5.1 | 无 | ✅ 2026-08-10 |

**P0 验收**：大文件下载有进度与提速；大目录 ZIP 立即开始下载；列表接口响应减小；storage 接口毫秒级；界面整体焕新且不影响既有功能。

**P0 落地说明（2026-08-10）**：新增 `frontend/src/utils/downloader.js`（并发 Range 下载 + 暂停/继续/重试 + 能力回退）与 `App.vue` 右下角悬浮下载面板；`download_service.iter_zip_stream()` 实现边打包边发送（后台线程 + 增量读取，`StreamingResponse`）；`SelectiveGZipMiddleware` 仅压缩 text/json 响应（避免破坏大文件 Range/Content-Length）；`storage_info()` 加 30s TTL 缓存并在上传/删除/重命名/建目录后主动失效；视觉层采用 `#2563EB` 蓝紫渐变品牌头 + 卡片化布局 + CSS 变量主题（`style.css` 覆盖 Element Plus 变量）。测试从 16 项增至 **20 项全绿**。

### Phase 1 —— 交互功能补齐（预计 4~5 天）

| # | 任务 | 归属 | 依赖 |
| --- | --- | --- | --- |
| 1 | 文件夹上传 | 4.1 | P0 |
| 2 | 全局传输中心 + 暂停/重试/通知 | 4.2 | P0-1 |
| 3 | 刷新后上传任务恢复 | 4.3 | P0 |
| 4 | 预览增强（图片缩放/翻页/编码） | 4.4 | P0 |
| 5 | 快捷键 / 搜索防抖 / 全选 / 右键菜单 | 4.5 | P0 |
| 6 | 深色模式 | 5.2 | P0-5 |

**P1 验收**：文件夹一键上传；所有传输在悬浮面板可见可控；刷新可续传；图片预览缩放翻页；深色模式一键切换并记住。

### Phase 2 —— 视觉丰富与完善（预计 3~4 天）

| # | 任务 | 归属 | 依赖 |
| --- | --- | --- | --- |
| 1 | 网格视图 + 缩略图接口 | 5.3 | P0-5 |
| 2 | 存储仪表盘（环形图 + 类型分布） | 5.4 | P0-4 |
| 3 | 空状态插画与微动效 | 5.5 | P0-5 |
| 4 | 移动端适配优化 | 5.6 | P1-2 |
| 5 | 全量回归 + 文档更新 | 7 | 全部 |

**P2 验收**：双视图可切换且图片有缩略图；footer 可视化；空状态与动效到位；手机浏览器体验流畅。

---

## 七、验收标准与回归测试

### 7.1 功能验收清单

- [ ] 大文件下载显示实时进度/速度，可暂停/继续，失败自动重试
- [ ] 不支持分片下载的浏览器回退到普通下载，功能不降级
- [ ] 大目录 ZIP 打包首字节立即返回，且文件完整可解压
- [ ] `/api/storage` 大目录毫秒级返回，上传/删除后数据正确
- [ ] 文件夹（嵌套目录）上传成功，路径结构正确
- [ ] 传输中心可查看/暂停/重试所有任务，通知按授权工作
- [ ] 刷新页面后检测并恢复未完成任务（或合理清理失效任务）
- [ ] 图片预览支持缩放/旋转/相邻翻页；文本预览编码正确
- [ ] 深色/浅色模式切换即时生效并持久化
- [ ] 网格视图缩略图正确加载，无缩略图回退图标
- [ ] 移动端小屏下操作可用、无横向溢出

### 7.2 回归测试要求

- 后端所有改动同步更新/新增 `tests/test_api.py` 用例，沿用"临时 `DATA_DIR` + 手动 `init_db()`"隔离模式；
- **必须新增**：ZIP 流式下载完整性测试（对比输出文件字节数/MD5）、Range 分片并发请求测试、storage 缓存失效测试、缩略图路径穿越防护测试（复用 `safe_join` 校验）；
- 全量 `pytest tests/ -v` 保持 **≥20 项全绿**（现有 16 项 + 新增）；
- 前端构建 `npm run build` 通过，`emptyOutDir: false` 配置保持不变。

---

## 八、风险控制与依赖

| 风险 | 影响 | 缓解措施 |
| --- | --- | --- |
| 流式 ZIP 异常导致连接中断 | 用户下载失败 | 逐文件 try/except 跳过坏文件；异常时清理临时文件；新增完整性测试 |
| 分片下载内存占用 | 超大文件前端 Blob 累积 | 超过阈值（如 2GB）自动回退跳转下载；分片数量随文件大小动态调整 |
| Pillow 新增依赖 | 部署环境需安装 | 写入 `requirements.txt`；缩略图失败静默回退类型图标，不阻塞主流程 |
| 传输中心状态耦合 | 逻辑复杂、回归风险 | 独立 `transfers.js` 模块管理，UploadPanel 逐步接入，保留原弹窗模式 |
| 深色模式改造范围大 | 大量样式调整 | 先 CSS 变量化（P0-5）再做深色（P1-6），分两步降低风险 |
| 移动端 WebView 不支持分片下载 | 下载功能降级 | 能力检测 + 自动回退，行为等同现状 |

### 依赖清单（新增）

- 后端：`Pillow`（缩略图）——写入 `requirements.txt`
- 前端：无新增依赖（`ElImageViewer` 来自 Element Plus 自带组件；图表用 SVG 手写）
- 浏览器：`fetch` + `Range`（现代浏览器均支持）

---

*本文档随实现进度更新；涉及文件路径均以当前仓库结构为准。*
