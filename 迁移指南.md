# LanShare 项目迁移指南

**目的**：
- **源码迁移**：把这个项目完整迁移到其他电脑，**继续开发**。
- **打包发布**：打包成独立 exe，**零依赖**分发给家人 / 同事直接使用。

---

## 一、打包发布（给别人用，推荐）

### 1. 打包一次，永久复用

在**开发机**上执行（只需一次，Windows）：

```
双击「打包.bat」
```

或命令行：

```bash
python -m PyInstaller LanShare.spec --noconfirm
```

脚本会自动完成：
1. 安装 PyInstaller（国内镜像加速）
2. 前端产物缺失时自动构建
3. 生成 **`dist/LanShare.exe`**（约 40MB，单文件，含完整前端页面）

> 修改了前端代码后重新打包：运行 `打包.bat rebuild`（强制重建前端再打包）。

### 1.5 进阶：制作成标准安装包（Setup.exe，小白友好）

想让它"像正常软件一样"安装（有安装向导、桌面快捷方式、可卸载），再执行一步：

```
双击「打包安装包.bat」
```

脚本会自动下载安装 Inno Setup（免费，约 2MB，装一次即可），然后编译出 **`installer/LanShare-Setup.exe`**。

**安装版的体验**：双击 → 中文向导下一步 → 完成 → 桌面/开始菜单自动出现 LanShare 图标，控制面板可卸载。数据保存在 `%LOCALAPPDATA%\LanShare`（安装版自动回退，不依赖 exe 位置）。

> 自动下载失败时，手动到 https://github.com/jrsoftware/issrc/releases 下载 `innosetup-6.7.3.exe` 安装后重试即可。

### 2. 分发给别人怎么用

只需把 **`dist/LanShare.exe`** 这一个文件发给对方（微信 / U盘 / 网盘），对方：

1. 把 exe 放到任意文件夹（建议单独建一个目录）
2. **双击运行** —— 无需安装 Python / Node / 任何依赖
3. 弹出的窗口会打印局域网 / 本机访问地址，浏览器打开即可用，手机扫码也可

### 3. 打包版的数据在哪

- 上传的文件保存在 **exe 同目录的 `data/`** 下（`data/files/` 是正式文件，`data/lanshare.db` 是元数据）
- 备份 = 拷贝 exe 旁的 `data/` 文件夹；删除该文件夹 = 彻底清除数据
- 换端口 / 设访问密码：启动前在命令行设置环境变量，如 `set LANSHARE_PORT=9000`、`set LANSHARE_PASSWORD=123456`，再运行 exe

### 4. 常见问题

| 问题 | 说明 |
| --- | --- |
| 杀毒软件误报 | PyInstaller 打包的程序偶尔被误报，添加信任即可 |
| 双击没反应 / 端口被占 | 可能是旧实例未退出，任务管理器结束 LanShare.exe 后再试 |
| 换到 Mac / Linux 使用 | 需在对应系统上重新打包（代码本身跨平台） |

---

## 二、迁出（这台电脑）

整个 `LanShare-Source` 文件夹（或解压 zip）就是迁移载体：

- ✅ 包含：后端源码 `app/`、前端源码 `frontend/src/`、测试、依赖清单、启动脚本、开发指南
- ❌ 不含：node_modules、你的个人数据（`data/` 是空的）、AI 工作区记录

> 如果想把**当前已上传的文件**也带走，额外拷贝本机 `data/files/` 和 `data/lanshare.db` 到目标电脑的 `LanShare-Source/data/` 即可。

**发送方式**：`LanShare-Source.zip` 通过微信 / 网盘 / U盘 发给对方。

---

## 三、迁入（目标电脑）

### 方式 1：仅部署使用（最快）

1. 解压 `LanShare-Source.zip`
2. 双击 **`启动.bat`**（自动检测 Python、自动装依赖、自动启动）
3. 浏览器打开 `http://127.0.0.1:8000`，手机扫码即可用

### 方式 2：开发模式（改代码 + 热重载）★ 继续开发推荐

1. 解压后，确认已装 **Python 3.10+** 和 **Node.js 18+**
2. 双击 **`开发模式.bat`**（自动装前后端依赖、启动后端）
3. 另开终端进入 `frontend` 目录运行 `npm run dev`
4. 浏览器打开 `http://localhost:5173` —— 改 `frontend/src/` 代码保存即刷新

---

## 四、环境要求（目标电脑）

| 工具 | 版本 | 用途 |
| --- | --- | --- |
| Python | 3.10+ | 后端（必须） |
| Node.js | 18+ | 前端开发（开发模式需要，纯部署不需要） |

> 使用打包好的 `dist/LanShare.exe` 时，上述工具**都不需要**。

---

## 五、项目结构速览

```
LanShare-Source/
├── 启动.bat           ← 双击：一键部署运行
├── 开发模式.bat        ← 双击：开发环境（装依赖+启动后端）
├── 打包.bat           ← 双击：一键打包成独立 exe（需已装 Python + Node）
├── 打包安装包.bat      ← 双击：制作标准安装包 Setup.exe（需已装 Inno Setup）
├── LanShare.spec      ← PyInstaller 打包配置（单文件，内嵌前端）
├── installer.iss      ← Inno Setup 安装器脚本（中文向导+快捷方式+卸载）
├── ChineseSimplified.isl ← 安装器中文语言文件（编译时用，放项目根目录）
├── launcher.py        ← 打包版入口（frozen 环境启动）
├── 开发指南.md         ← 必读：如何开发/加接口/加页面/常见问题
├── app/               ← 后端 FastAPI 源码
├── frontend/
│   ├── src/           ← 前端 Vue3 源码
│   └── dist/          ← 已构建的前端（部署时后端自动托管）
├── data/              ← 运行后自动生成（上传的文件 + 数据库）
├── tests/             ← pytest 后端测试（20 项）
├── dist/              ← 打包产物输出目录（LanShare.exe）
└── requirements.txt   ← 后端依赖
```

---

## 六、验证是否迁移成功

```bash
# 目标电脑上执行
python -m pytest tests/ -v
# 预期: 20 passed
```

---

*配套阅读：《开发指南.md》——完整的代码结构、新增 API/页面教程、配置项、常见问题。*
