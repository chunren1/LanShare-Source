# LanShare 开发指南（给项目协作者）

欢迎加入 LanShare 项目！本指南带你完成**本地部署**和**自由开发**。

---

## 一、环境要求

| 工具 | 版本 | 用途 |
| --- | --- | --- |
| Python | 3.10+ | 运行后端 |
| Node.js | 18+ | 构建/开发前端（**部署只跑后端时可不装**） |

> 验证环境：`python --version`、`node --version`（有输出即 OK）

---

## 二、本地部署（先跑起来）

```bash
# 1. 安装后端依赖
pip install -r requirements.txt

# 2. 启动后端（会同时托管已构建好的前端页面）
python -m app.main
```

启动后：
- 本机访问：`http://127.0.0.1:8000`
- 局域网访问：终端会打印地址，如 `http://192.168.x.x:8000`（手机扫码首页二维码即可用）

> 看到 `Uvicorn running on http://0.0.0.0:8000` 即部署成功。

---

## 三、开发模式（改代码即时生效）

开发前端需要热重载，用两个终端：

```bash
# 终端 1：启动后端（提供 API）
python -m app.main

# 终端 2：启动前端开发服务器（热重载，改代码自动刷新）
cd frontend
npm install          # 首次需要
npm run dev
```

- 打开 `http://localhost:5173` 进入开发模式
- Vite 已配置代理：前端请求 `/api/*` 自动转发到后端 8000 端口
- 修改 `frontend/src/` 下的代码保存即生效

**前端构建**（部署前需要）：
```bash
cd frontend && npm run build
# 产物输出到 frontend/dist/，后端启动时自动托管
```

---

## 四、项目结构

```
lanshare/
├── app/                        # 后端（FastAPI）
│   ├── main.py                 # 应用入口：路由注册、鉴权中间件、前端托管
│   ├── config.py               # 配置：端口/存储目录/密码/分片大小（环境变量可覆盖）
│   ├── database.py             # SQLite 连接与建表
│   ├── utils.py                # 路径安全防护、MD5、文件名清洗
│   ├── routers/                # API 路由层（HTTP 层）
│   │   ├── files.py            #   文件列表/删除/重命名/新建/搜索
│   │   ├── upload.py           #   分片上传/合并/秒传
│   │   ├── download.py         #   下载/打包
│   │   └── auth.py             #   访问密码
│   └── services/               # 业务逻辑层
│       ├── file_service.py     #   文件操作核心
│       ├── upload_service.py   #   分片/断点续传核心
│       └── download_service.py #   流式下载/ZIP 打包
├── frontend/                   # 前端（Vue 3 + Vite + Element Plus）
│   ├── src/
│   │   ├── App.vue             # 主界面（文件列表/预览/二维码等）
│   │   ├── main.js             # 入口
│   │   ├── api/index.js        # 所有 API 调用封装
│   │   ├── components/         # 组件（UploadPanel 上传面板等）
│   │   └── utils/              # md5.js 计算指纹 / uploader.js 分片上传引擎
│   └── dist/                   # 前端构建产物（后端托管）
├── data/                       # 运行时生成：上传的文件 + lanshare.db（勿提交）
├── tests/                      # pytest 后端测试
└── requirements.txt            # 后端依赖
```

---

## 五、如何新增功能

### 后端：加一个新 API

1. 在 `app/services/` 建/改业务逻辑（如 `file_service.py`）
2. 在 `app/routers/` 对应路由文件加接口，或新建 router：
   ```python
   # app/routers/xxx.py
   from fastapi import APIRouter, HTTPException
   router = APIRouter(prefix="/api/xxx", tags=["xxx"])

   @router.get("/hello")
   def hello():
       return {"msg": "world"}
   ```
3. 在 `app/main.py` 注册：`app.include_router(xxx.router)`
4. 重启后端生效（开发时可加 `--reload`）

### 前端：加一个页面/组件

1. 在 `frontend/src/components/` 新建 `.vue` 组件
2. 在 `App.vue`（或目标页面）import 并使用
3. `npm run dev` 下保存即生效

### 跑测试

```bash
pip install pytest httpx
pytest tests/ -v        # 当前 16 项，全绿
```

---

## 六、配置项（环境变量）

| 变量 | 说明 | 默认 |
| --- | --- | --- |
| `LANSHARE_PORT` | 端口 | 8000 |
| `LANSHARE_DATA_DIR` | 数据存储目录 | `./data` |
| `LANSHARE_PASSWORD` | 访问密码（空=不启用） | 空 |
| `LANSHARE_CHUNK_SIZE` | 分片大小 | 4MB |
| `LANSHARE_MAX_FILE_SIZE` | 单文件上限 | 100GB |

```bash
LANSHARE_PASSWORD=123456 python -m app.main   # 开启访问密码
```

---

## 七、数据与备份

- 上传的文件在 `data/files/`，元数据在 `data/lanshare.db`
- 备份 = 拷贝这两个位置；删除 `data/` 即恢复初始状态

---

## 八、常见问题

| 问题 | 解决 |
| --- | --- |
| 端口 8000 被占用 | `LANSHARE_PORT=8080 python -m app.main` |
| `pip install` 慢 | `pip install -r requirements.txt -i https://pypi.tuna.tsinghua.edu.cn/simple` |
| `npm install` 慢 | `npm install --registry=https://registry.npmmirror.com` |
| 手机无法访问 | 确认同一局域网；Windows 防火墙放行 8000 端口 |
| 前端改了不生效 | 检查是否在 `npm run dev`（5173）而非直接访问 8000；部署前需 `npm run build` |

---

*有任何问题，欢迎随时沟通。Happy coding! 🚀*
